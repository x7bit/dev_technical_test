/**
 * En base a un array de números de entrada y un número objetivo, se tiene que devolver un array
 * de pares que sumen el número objetivo utilizando únicamente los números contenidos en el array
 * de entrada, el cual no tiene por qué estar ordenado y no debe de tener números repetidos.
 *
 * Así por ejemplo para un array [7, 5, 1, 3] y un número objetivo igual a 8, la solución sería:
 *    [[7, 1], [5, 3]]
 *
 * Adicionalmente se piden estos requerimientos:
 *  - Sólo se puede iterar el array de números una única vez (eso incluye tanto bucles como
 *      funciones prototipo de arrays como forEach, find, filter, map, etc.).
 *  - No se pueden repetir pares sin importar su orden, [3, 8] y [8, 3] se consideran repetidos.
 *  - El orden de los números en los pares debe respetar el mismo orden que tenían dentro del
 *      array de números.
 *  - Se tiene que hacer todo usando únicamente esta función 'getPairs'.
 *
 * Tiempo estimado máximo: 20 minutos.
 *
 * @param {array} numArray Array de números de entrada
 * @param {number} target Número objetivo
 * @returns {array} Array de pares, donde cada par es un array de tamaño 2
 */
export const getPairs = (numArray, target) => {
  //
};
